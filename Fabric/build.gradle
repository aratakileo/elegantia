plugins {
    id 'multiloader-loader'
    id 'fabric-loom'
}

repositories {
    maven { url "https://maven.terraformersmc.com/releases/" }
}

dependencies {
    minecraft "com.mojang:minecraft:${minecraft_version}"

    mappings loom.officialMojangMappings()

//    mappings loom.layered {
//        officialMojangMappings()
//        parchment("org.parchmentmc.data:parchment-${parchment_minecraft}:${parchment_version}@zip")
//    }

    modApi("com.terraformersmc:modmenu:17.0.0-alpha.1") { exclude(module: "fabric-api") }

    modImplementation "net.fabricmc:fabric-loader:${project.fabric_loader_version}"
    modImplementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_version}"


    annotationProcessor project(":Common")
}

loom {
     mixin {
        defaultRefmapName = "${mod_id}.refmap.json"
         useLegacyMixinAp = true
     }

    runs {
        client {
            client()
            setConfigName('Fabric Client')
            ideConfigGenerated(true)
            runDir('runs/client')
        }

        server {
            server()
            setConfigName('Fabric Server')
            ideConfigGenerated(true)
            runDir('runs/server')
        }
    }
}

compileJava {
    options.compilerArgs += [
            "-Amixin.target.mappings=mojang"
    ]
}

sourceSets {
    main {
        java.srcDirs += [project(":Common").sourceSets.main.java.srcDirs]
        resources.srcDirs += [project(":Common").sourceSets.main.resources.srcDirs]
    }
}